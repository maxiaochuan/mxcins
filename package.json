{
  "name": "root",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "site": "vite build",
    "dev": "vite",
    "dev:res": "run-p dev:res:**",
    "dev:res:webapi": "pnpm run -r --filter webapi dev",
    "dev:res:hooks": "pnpm run -r --filter hooks dev",
    "dev:res:components": "pnpm run -r --filter components dev",
    "clean": "run-s clean:**",
    "clean:res:webapi": "pnpm run -r --filter webapi clean",
    "clean:res:hooks": "pnpm run -r --filter hooks clean",
    "clean:res:components": "pnpm run -r --filter components clean",
    "format": "run-s format:**",
    "format:res:webapi": "yarn workspace @mxcins/webapi run rescript format -all",
    "format:res:hooks": "yarn workspace @mxcins/hooks run rescript format -all",
    "format:res:components": "yarn workspace @mxcins/components run rescript format -all",
    "icons": "node scripts/icons.cjs",
    "prettier": "prettier --write '**/*.{json,ts,tsx,js,jsx}'",
    "publish": "lerna publish from-git",
    "version": "lerna version --conventional-commits --yes"
  },
  "dependencies": {
    "@ant-design/icons": "4.7.0",
    "@rescript/react": "0.10.3",
    "@types/react": "17.0.39",
    "@types/react-dom": "17.0.13",
    "react": "17.0.2",
    "react-dom": "17.0.2",
    "rescript": "10.0.1",
    "twind": "0.16.16"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "1.2.0",
    "prettier": "2.5.1",
    "gentype": "4.3.0",
    "lerna": "4.0.0",
    "npm-run-all": "4.1.5",
    "react-router": "^6.2.2",
    "react-router-dom": "^6.2.2",
    "typescript": "^4.6.2",
    "vite": "2.8.6"
  }
}
