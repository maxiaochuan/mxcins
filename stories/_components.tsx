import {
  Button as B,
  ButtonProps,
  Card as C,
  CardProps,
} from "@components";

export const Button = (props: ButtonProps) => <B {...props} />
export const Card = (props: CardProps) => <C {...props} />